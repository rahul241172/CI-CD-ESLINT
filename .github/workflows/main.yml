name: Run ESLINT 
on: push
jobs:
      eslint:
        runs-on: ubuntu-latest
        steps:
        - name: Install dependencies
          run: npm i
        - name: Run eslint
          run: npm i eslint
